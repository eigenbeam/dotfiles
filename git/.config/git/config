[user]
	name = Kevin W. Beam
	email = kevin.beam@colorado.edu
[color]
	ui = auto
[apply]
	whitespace = nowarn
[core]
	autocrlf = input
	safecrlf = false
	pager = delta
	excludesFile = ~/.config/git/ignore
	# Uncomment to use global hooks (overrides per-repo hooks):
	# hooksPath = ~/.config/git/hooks
[alias]
	st = status
	sts = status --short
	hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
	co = checkout
	br = branch
	ci = commit
	unstage = restore --staged
	amend = commit --amend --no-edit
	undo = reset --soft HEAD~1
	prev = switch -
[push]
	default = upstream
	followTags = true
[pull]
	rebase = true
[fetch]
	prune = true
[rebase]
	autoStash = true
	autoSquash = true
[rerere]
	enabled = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
# Credential helper is machine-specific; set in ~/.gitconfig-local
# macOS: [credential] helper = osxkeychain
# Linux: [credential] helper = cache  (or store, or /usr/lib/git-core/git-credential-libsecret)
[github]
	user = eigenbeam
[interactive]
	diffFilter = delta --color-only
[delta]
	navigate = true
	side-by-side = true
	line-numbers = true
	syntax-theme = GitHub
	light = true
	minus-style = syntax "#EACED1"
	minus-emph-style = syntax "#E0BFC3"
	plus-style = syntax "#D5E0C5"
	plus-emph-style = syntax "#CBDAB8"
[diff]
	colorMoved = default
[merge]
	conflictStyle = zdiff3
[init]
	defaultBranch = main

# Machine-specific overrides (e.g., work email, git-secrets)
[include]
	path = ~/.gitconfig-local

# Directory-based overrides (uncomment and adjust for per-repo-directory config)
# [includeIf "gitdir:~/work/"]
#	path = ~/.gitconfig-work
